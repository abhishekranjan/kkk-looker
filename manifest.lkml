# ============================================================================
# FILE: manifest.lkml  (project root)
# Map layers now live in the two model files, so this only names the project.
# ============================================================================

project_name: "central_dashboard"
